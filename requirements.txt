# Core MCP + Azure client libraries used throughout the notebooks/examples
mcp>=1.2.0
azure-identity>=1.19.0
openai>=1.50.0
python-dotenv>=1.0.1

# Notebook tooling
jupyter>=1.0.0
ipykernel>=6.29.0
nbformat>=5.10.0

# Pretty console output used by the CLI examples
rich>=13.7.0

# Optional: PyPI-distributed Azure MCP Server binary launcher
# (the npm package @azure/mcp is used in most examples instead)
msmcp-azure>=1.0.0
